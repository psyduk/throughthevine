var assert = require("assert"),
    parse  = require('./../parse.js');

describe('Parsing', function(){
  describe('basic tests', function(){
    it('should have basic methods', function(){
      assert.equal(true, true);
    });
  });
});